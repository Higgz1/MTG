import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'higgz.mtg.project',
  appName: 'MTG',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
